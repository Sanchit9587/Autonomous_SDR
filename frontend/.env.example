# Base URL of the FastAPI backend. Leave blank to use the Vite dev proxy
# (recommended for local dev — see vite.config.ts). Set to a full URL for
# a deployed backend, e.g. https://your-api.onrender.com
VITE_API_BASE_URL=